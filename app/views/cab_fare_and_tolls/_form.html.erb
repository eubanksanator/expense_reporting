<%= form_for(@cab_fare_and_toll) do |f| %>
  <% if @cab_fare_and_toll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cab_fare_and_toll.errors.count, "error") %> prohibited this cab_fare_and_toll from being saved:</h2>

      <ul>
      <% @cab_fare_and_toll.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :occurence %><br>
    <%= f.date_select :occurence %>
  </div>
  <div class="field">
    <%= f.label :cost %><br>
    <%= f.number_field :cost %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
