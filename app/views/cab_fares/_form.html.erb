<%= form_for(@cab_fare) do |f| %>
  <% if @cab_fare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cab_fare.errors.count, "error") %> prohibited this cab_fare from being saved:</h2>

      <ul>
      <% @cab_fare.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :occurence %><br>
    <%= f.date_select :occurence %>
  </div>
  <div class="field">
    <%= f.label :cost %><br>
    <%= f.number_field :cost %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: "text-box-size" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
