<%= form_for(@other_expense) do |f| %>
  <% if @other_expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@other_expense.errors.count, "error") %> prohibited this other_expense from being saved:</h2>

      <ul>
      <% @other_expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :occurence %><br>
    <%= f.date_select :occurence %>
  </div>
  <div class="field">
    <%= f.label :cost %><br>
    <%= f.text_field :cost, placeholder: '$' %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
